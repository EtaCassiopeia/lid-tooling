# Arrow: url-validator

URL validation logic — rejects non-HTTP/HTTPS schemes, oversized URLs, and control characters.

## Status

**MERGED** — absorbed into `shortener-core` at PR #7. Validation is now a first-class responsibility of `shortener-core` (USH-CORE-006..008).

## References

### HLD
- `docs/high-level-design.md` § Approach / shortener-core
